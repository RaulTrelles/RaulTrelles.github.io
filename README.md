# PortafolioRaulTrelles

● Bash(git stash)
● Bash(git pull)
● Bash(git stash pop)
● Bash(git add .)
● Bash(git commit -m "Fix contact form bug, correct ERP page titles, correct HTML tag nesting, and clean up styles")
● Bash(git push origin main) (ctrl+o to expand)
